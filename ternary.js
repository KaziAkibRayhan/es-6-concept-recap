const isHotDay = true;

if (isHotDay === true) {
    console.log('today is hot');
}
else{
    console.log('today is not hot');
}

const today = (isHotDay === true) ? 'today is hot': 'today is not hot';
console.log(today);